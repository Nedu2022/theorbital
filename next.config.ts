import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Removed output: "export" to enable Vercel's full Next.js support
  // including API routes and WebSocket handling
};

export default nextConfig;
